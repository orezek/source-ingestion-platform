# Start URL Validation

This test verifies the start URL textarea enforces URL validity and a bounded URL count.

Focus:

- Reject malformed URL lines.
- Reject oversized URL lists to limit payload abuse.
